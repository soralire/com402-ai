Worker/credit oversubscription experiment for cxl_numa_csma

Model:
  CPU workers -> CXL Switch queue -> Type-3 Memory Device -> NUMA remote memory backend

Fixed/default configuration:
  bin=/home/sjk/com402-ai/cxl_numa_csma
  duration=15
  mem_node=1
  cpu_node=0
  mem_mb=512
  touches_per_req=4096

Sweep configuration:
  modes=0 1 2
  loads=100
  seeds=1 2 3 4 5
  threads=1 2 4 8 16 32
  queue_depths=4
  device_workers=1
  total_runs=90

Interpretation:
  oversubscription = threads / queue_depth.
  oversubscription <= 1 is the low-contention region.
  oversubscription > 1 creates competition for fixed fabric credits.
  Random is interpreted as the blocking/no-rejection baseline.
  CSMA/AIMD retain the same logical request across admission retries.
  Each AIMD worker owns at most one submitted request.
  AIMD device completion immediately releases its global in-flight slot.
  Their p99 values include admission waiting and backoff.
  acceptance rate means successful fabric admissions / admission attempts.
  Compare goodput, logical-request p99, attempt success rate, and retries per completion.
